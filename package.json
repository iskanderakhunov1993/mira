{
  "name": "mira",
  "private": true,
  "version": "1.3.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@mira/web",
    "build": "npm run build --workspace=@mira/web",
    "lint": "npm run lint --workspace=@mira/web",
    "start": "npm run start --workspace=@mira/web"
  }
}
